<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.postit.mymomsweather">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CALL_LOG"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_pink"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_pink_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyMomsWeather">
        <activity
            android:name=".presentation.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>
        <activity android:name=".presentation.parentregister.ParentRegisterActivity"/>
        <activity android:name=".presentation.profile.ProfileActivity"/>
        <activity android:name=".presentation.calendar.CalendarActivity"/>
        <activity android:name=".presentation.community.CommunityActivity"/>
        <activity android:name=".presentation.game.GameActivity"/>
        <activity android:name=".presentation.signup.SignUpActivity"/>
        <activity android:name=".presentation.myinfo.MyInfoActivity"/>
    </application>

</manifest>